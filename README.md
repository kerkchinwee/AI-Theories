INTRODUCTION

This project serves as a playground for the developer (Kerk Chin Wee)
to verify the AI knowledge learnt, enhanced or even created
by practicing the knowledge in read code in any interested area.

In order to make the project functions well, the required dependencies
need to be installed before use. The dependencies should be installed
by referring the "requirements.txt" in the same folder:

    "pip install -r requirements.txt"